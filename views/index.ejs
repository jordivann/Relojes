<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Seleccionar Sucursal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1>Seleccionar Sucursal y Rango de Fechas</h1>
        <form action="/fetch-attendance" method="POST">
            <div class="mb-3">
                <label for="branch" class="form-label">Selecciona la sucursal:</label>
                <select id="branch" name="branch" class="form-select" required>
                    <% branches.forEach(branch => { %>
                        <option value="<%= branch.name %>">
                            <% if (branch.name === 'Administración') { %>
                                👑 <%= branch.name %> (Especial)
                            <% } else { %>
                                <%= branch.name %>
                            <% } %>
                        </option>
                    <% }) %>
                </select>
            </div>
            <div class="mb-3">
                <label for="startDate" class="form-label">Fecha de inicio:</label>
                <input type="date" id="startDate" name="startDate" class="form-control" required>
            </div>
            <div class="mb-3">
                <label for="endDate" class="form-label">Fecha de fin:</label>
                <input type="date" id="endDate" name="endDate" class="form-control" required>
            </div>
            <button type="submit" class="btn btn-primary">Obtener Asistencia</button>
        </form>
    </div>
</body>
</html>
